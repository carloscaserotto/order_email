<h1>Edit an user</h1>
  
  <%= form_with(model: @user, local: true)  do |f| %>
  <p> 
    <%= f.label :email %><br/> 
    <%= f.email_field :email %>
  </p>
  <p>
    <%= f.submit %> 
  </p>
  <% end %>